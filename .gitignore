# Node / Expo
node_modules/
.expo/
dist/
web-build/
*.log
npm-debug.*
yarn-error.*

# Env
.env
.env.local

# OS
.DS_Store

# Agent folders (may contain local credentials/artifacts)
.claude/settings.local.json

# Claude Design: keep .dc.html sources + canvas.json, ignore the seeded/built payload
design/**/*.html
!design/**/*.dc.html
